<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <TextView
        android:id="@+id/tv_question"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:textStyle="bold"
        android:textColor="@color/md_theme_light_onSurface" />

    <!--
        FlexboxLayout replaces RadioGroup here so that options stay horizontal
        and automatically wrap to the next row when they overflow the screen width.
        Single-selection is enforced in QuestionRenderer.showRadio().
    -->
    <com.google.android.flexbox.FlexboxLayout
        android:id="@+id/rg_options"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        app:flexDirection="row"
        app:flexWrap="wrap"
        app:alignItems="center" />

    <TextView
        android:id="@+id/tv_error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginStart="4dp"
        android:textColor="?attr/colorError"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:visibility="gone" />

</LinearLayout>
