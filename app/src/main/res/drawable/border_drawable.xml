<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color inside CardView -->
    <stroke
        android:width="2dp"
        android:color="@android:color/holo_red_light"/> <!-- Stroke (border) color and width -->
    <corners android:radius="4dp"/> <!-- Corner radius for rounded edges -->
</shape>